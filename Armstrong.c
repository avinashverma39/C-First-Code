// PROGRAM TO CHECK NUMBER IS ARMSTRONG OR NOT
#include <stdio.h>
int main()
{

    int num, num2 = 0, temp, digit, power;

    printf("Enter the number: ");
    scanf("%d", &num);
     while()

    return 0;
}